services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mapa_poder
      POSTGRES_USER: mapa
      POSTGRES_PASSWORD: mapa
    ports:
      - "5432:5432"
    volumes:
      - mapa_postgres:/var/lib/postgresql/data

volumes:
  mapa_postgres:
